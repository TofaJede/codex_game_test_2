# codex_game_test_2

This repository contains a very small top-down shooter implemented in a single HTML file.

## Usage
Open `index.html` in your browser. Use `W`, `A`, `S`, and `D` to move the player. Aim with the mouse and left-click to fire projectiles toward the mouse pointer.
